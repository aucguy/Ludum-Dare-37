base.registerModule('sbl.compiler', function() {
  var util = base.importModule('util');
  var exectur = base.importModule('executor');
  
  function compile() {
    //TODO
  }
  
  return {
    compile: compile
  }
});